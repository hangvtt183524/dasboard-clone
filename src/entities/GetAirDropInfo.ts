export interface IAirDropInfo {
    address?: string,
    amount?: string,
    reward?: string,
    received?: string,
    action?: number,
    is_eligible?: boolean,
    merkleproof?: string[],
    id?: number
}
