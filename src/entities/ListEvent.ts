export interface IListEvents {
    event_id?: number,
    event_name?: string,
    event_type?: number,
    event_description?: string,
    event_status_id?: number,
    event_status?: string,
    event_start: string,
    event_end: string,
    is_reg?: boolean,
    event_announcement?: string,
}